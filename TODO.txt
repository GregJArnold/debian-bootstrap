generate SSH keys
upload them to whereever

FireFox:
DDG
Multi Account Container
Facebook Container
React Dev Tools
Redux Dev Tools

Upon cloning repos:
git config remote.origin.prune true
git config remote.origin.tagOpt --tags

Others:
install thefuck

Ansible:
- Fetch the PG unixauth password from ... somewhere? and put it in the file.
- Convert the rest to Ansible
